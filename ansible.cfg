[defaults]
inventory = environments/localVM/hosts
roles_path = roles:./kubespray/roles
# Lab: accept new node host keys on first connect (avoid manual ssh-keyscan per hostname/IP)
host_key_checking = False

[privilege_escalation]
become = True
become_method = sudo
