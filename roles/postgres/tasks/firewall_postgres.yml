---
# Postgres: apply firewall role with Postgres/DB ports.

- name: Apply firewall for Postgres
  include_role:
    name: firewall
  vars:
    firewall_ports: "{{ postgres_firewall_ports }}"
  tags: [postgres, firewall]
