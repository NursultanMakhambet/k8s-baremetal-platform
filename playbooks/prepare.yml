---
# Prepare nodes (firewall etc.). Tags: k8s (Kubernetes ports), postgres (Postgres/DB ports), firewall (both).
# Hosts: k8s and db. Use TAGS to run only selected parts.

- hosts: k8s,db
  become: true
  roles:
    - prepare_nodes
